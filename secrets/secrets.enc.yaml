# This file should be encrypted with SOPS before committing.
# Run: sops -e -i secrets/secrets.enc.yaml
#
# To encrypt for the first time:
# 1. Generate age keys: age-keygen -o keys.txt
# 2. Update .sops.yaml with your public key
# 3. Run: sops -e -i secrets/secrets.enc.yaml

pg_root_password: CHANGE_ME_postgres_root_password
pg_n8n_password: CHANGE_ME_n8n_db_password
pg_memory_password: CHANGE_ME_memory_db_password
pg_audit_password: CHANGE_ME_audit_db_password
redis_password: CHANGE_ME_redis_password
n8n_encryption_key: CHANGE_ME_n8n_encryption_key
anthropic_api_key: CHANGE_ME_anthropic_api_key
